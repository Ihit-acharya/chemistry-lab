# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/chemistry-lab

# Admin Token for exports (optional)
ADMIN_TOKEN=your-secret-admin-token

# Environment
NODE_ENV=production

# Port (Render assigns this automatically)
PORT=3000
